const categories = ['work', 'shot', 'photo', 'slider', 'photos', 'contact']

const sitesByUserId = {
  'klimstepan.com': 1, // userId in DB
  'derzhanovska.com': 2,
}

module.exports = {
  categories,
  sitesByUserId
}